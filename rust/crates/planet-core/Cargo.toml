[package]
name = "planet-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Engine-independent planet code: cube-sphere convention, baked-cube loading"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
